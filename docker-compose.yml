version: '3.8'
services:
  storymind:
    build: .
    ports:
      - "4000:4000"
    environment:
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      - NODE_ENV=production
      - LOG_LEVEL=info
    volumes:
      - storymind-data:/app/server
    restart: unless-stopped

volumes:
  storymind-data:
